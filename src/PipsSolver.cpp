#include "pips/PipsSolver.hpp"

// The solver is fully defined in the header (templated). This translation unit
// intentionally has no additional definitions after the compact-tile / bitset
// rewrite removed the old Variable class.
